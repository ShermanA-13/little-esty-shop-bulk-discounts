<%= form_with url: merchant_bulk_discount_path(@merchant, @discount), method: :patch do |f| %>
  <%= f.label :percentage %>
  <%= f.text_field :percentage, value: @discount.percentage %>
  <%= f.label :threshold %>
  <%= f.text_field :threshold, value: @discount.threshold %>
  <%= f.submit "Submit" %>
<% end %>