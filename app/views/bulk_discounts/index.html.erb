<h1>All Discounts</h1>

  <% @merchant.bulk_discounts.each do |discount| %>
    <div id='discount-<%= discount.id %>'>
      <li>
        <%= link_to "#{discount.percentage} off of #{discount.threshold} or more items!", merchant_bulk_discounts_path(@merchant, discount.id) %>
      </li>
    </div>
  <% end %>